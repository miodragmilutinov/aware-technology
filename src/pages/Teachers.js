import React from "react";

import SideNav from "../SideNav";

import "../css/teachers.css";

const Teachers = () => {
  return (
    <div>
      <SideNav />
      <section className="teachers">
        <p className="header">Teachers</p>
      </section>
    </div>
  );
};

export default Teachers;
